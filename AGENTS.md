### Instructions

- I am using the uv package manager so use uv to run commands
with pip and python basically where ever it is needed use uv

- After each code edit explain what you did , why you did it and how you did it

- if you want to run any command in the CLI first activate the venv using the command `source .venv/bin/activate`
